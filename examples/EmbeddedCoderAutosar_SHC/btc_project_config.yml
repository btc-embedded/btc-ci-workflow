# ------------------------------------------------------------------
# Project-specific config file for BTC EmbeddedPlatform CI workflow
# 
# - this config is merged with a global config (if present)
# - the project-specific config has precedence in case of conflicts
# ------------------------------------------------------------------